name: Security Scan

on:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: "0 9 * * 1" # Mondays 9:00 UTC
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  security:
    name: Security Scan
    uses: zavestudios/platform-pipelines/.github/workflows/security-scan.yml@c0159f042136961310d5dce6ebe780805f7e9781
